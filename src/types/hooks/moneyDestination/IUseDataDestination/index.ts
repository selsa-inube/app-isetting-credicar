import { IEditData } from "../IEditData";

interface IUseDataDestination {
  id?: string;
  requestNumber?: string;
  option?: string;
  moneyDestinationData?: IEditData;
}

export type { IUseDataDestination };
